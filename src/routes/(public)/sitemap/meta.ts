/** Ultimo cambio de contenido de esta pagina (regla de frescura de AGENTS.md).
 * La /sitemap/ HTML mueve su fecha cuando se agrega una pagina a su lista; el ultimo
 * cambio real fue el commit de canonicals/trailing-slash. NO es un timestamp de build. */
export const contentUpdated = '2026-06-14';
