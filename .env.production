# Build-time PUBLIC env for `$env/static/public` (production mode: `vite build`).
# Value already public and committed in plaintext in wrangler.toml [vars], see the NOTE there.
PUBLIC_TURNSTILE_SITE_KEY="0x4AAAAAADdwuCX-HZK0twRi"
